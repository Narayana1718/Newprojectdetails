<aura:component implements="force:appHostable,flexipage:availableForAllPageTypes,flexipage:availableForRecordHome,force:hasRecordId,forceCommunity:availableForAllPageTypes,force:lightningQuickAction" access="global" >
    
    <aura:attribute name="firstname" type="string" default="Naresh"
                    description=" this is a field for emp firstname" />
    <aura:attribute name="lastname" type="string" default="Reddy"
                    description=" this is a field for emp lastname" />
    <aura:attribute name="salary" type="string" default="10000"
                    description=" this is a field for emp salary" />
    
    the name of employee we just stored is = {! v.firstname } | {! v.lastname } | {! v.salary }
    
</aura:component>